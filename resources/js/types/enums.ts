export enum MediaCollection {
    // Movie
    FrontCover = 'front_cover',
    FullCover = 'full_cover',
    // People
    Profile = 'profile',
    // Studio
    Logo = 'logo',
}

export enum EditSubroute {
    PrimaryFacts = 'primary_facts',
    AlternativeNames = 'aliases',
    Cast = 'cast',
    Scenes = 'scenes',
    ExternalIds = 'external_ids',
    Versions = 'versions',
}

export enum ProfileSubroute {
    Activity = 'activity',
    Favorites = 'favorites',
    Wishlist = 'wishlist',
    Collection = 'collection',
}
